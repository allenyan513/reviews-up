{
  "name": "@reviewsup/database",
  "version": "0.0.1",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "format": "prisma format",
    "lint": "eslint . --max-warnings 0"
  },
  "export": {
    ".": "./src/index.ts"
  }
}
